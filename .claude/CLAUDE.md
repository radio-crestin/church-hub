# graphify
- **graphify** (`.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, use the installed graphify skill or instructions before doing anything else.

# commits
- Never add `Co-Authored-By: Claude ...`, `Generated with Claude Code` or `Claude-Session` lines to a commit message. See the `commit-no-coauthor` skill and the "Commits — REQUIRED" section of the root CLAUDE.md.
